# --- Data collection ---
yt-dlp>=2024.1.1          # YouTube metadata search and video download
certifi>=2024.0.0         # CA bundle for SSL verification (yt-dlp HTTPS requests)

# --- Annotation tool + pose extraction ---
opencv-python>=4.9.0          # video frame reading
Pillow>=10.0.0                # converts OpenCV frames to tkinter-compatible images
# mediapipe>=0.10.0           # pose extraction (needed for preprocess/)

# --- Model training (needed for models/) ---
# torch>=2.2.0
# numpy>=1.26.0
# pandas>=2.2.0
# pyarrow>=15.0.0          # Parquet file format for keypoint storage

# --- Dev / testing ---
# pytest>=8.0.0
